<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required from Android 13; before that, notifications are allowed by default. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Read at the door only. Frames are decoded on the device and never leave it. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".ConnexaApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Connexa">
        <activity
            android:name=".feature.checkin.CheckInScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".feature.checkin.CheckInPassActivity"
            android:exported="false" />
        <activity
            android:name=".feature.auth.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".feature.auth.SignInActivity"
            android:exported="false" />
        <activity
            android:name=".feature.notifications.NotificationInboxActivity"
            android:exported="false" />
        <activity
            android:name=".feature.assistant.AssistantActivity"
            android:exported="false" />
        <activity
            android:name=".feature.organizer.OrganizerEventEditorActivity"
            android:exported="false" />
        <activity
            android:name=".feature.organizer.OrganizerDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".feature.events.EventDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".feature.events.EventFeedActivity"
            android:exported="false" />
        <activity
            android:name=".feature.calendar.CalendarActivity"
            android:exported="false" />
        <!-- The launcher entry carries the splash theme; it hands over to
             Theme.Connexa as the first frame is drawn. -->
        <activity
            android:name=".feature.foundation.MainActivity"
            android:theme="@style/Theme.Connexa.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives the device token and incoming notifications. Not exported: only
             the messaging library may start it. -->
        <service
            android:name=".core.push.ConnexaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
    </application>

</manifest>
