package com.connexa.mobile.feature.auth;

/** Fields represented by the authentication forms. */
public enum AuthField {
    FULL_NAME,
    EMAIL,
    PHONE_NUMBER,
    PASSWORD,
    PASSWORD_CONFIRMATION,
    ACCOUNT_ROLE,
    TERMS
}
