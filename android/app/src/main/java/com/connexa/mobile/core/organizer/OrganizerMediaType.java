package com.connexa.mobile.core.organizer;

/** The intended role of media attached to an organizer-managed event. */
public enum OrganizerMediaType {
    COVER_IMAGE,
    GALLERY_IMAGE,
    ATTACHMENT
}
