package com.connexa.mobile.core.organizer;

/** Fields that can receive immediate feedback while an organizer is preparing an event. */
public enum OrganizerDraftField {
    TITLE,
    DESCRIPTION,
    LOCATION,
    STARTS_AT,
    ENDS_AT,
    CATEGORY,
    CAPACITY
}
