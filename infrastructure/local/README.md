# Local infrastructure

Milestone 1 uses an in-memory event catalog and does not require a database, Firebase account, mail server, AI account, or notification provider.

This directory is reserved for reproducible local emulator or container configuration when a later milestone adds a stateful integration.
